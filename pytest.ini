[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    setup: simulate authorization via api
    need_review: marker for tests to be reviewed
    xfail: need for repair